# Math-pocket
小さな数学問題アプリのサンプル実装です。

使い方:

- このフォルダをローカルで開き、`index.html` をブラウザで開くか、簡易サーバで提供してください。

	```bash
	python3 -m http.server 8000
	# ブラウザで http://localhost:8000 を開く
	```

- 問題の画像ボタンをクリックすると、1〜13の数値を選ぶ画面に遷移します。
- 選択した値を式の `x` に代入して結果を表示します。

カスタマイズ:

- 問題や式は `app.js` の `problems` 配列で編集できます。

